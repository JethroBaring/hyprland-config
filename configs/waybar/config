[
  {
    "layer": "top", // Waybar on highest layer so tooltips go over windows
    "output": "eDP-1", // Set output to primary monitor
    "height": 27, // Set height to avoid jumping due to active workspace indicator

    "modules-left": ["clock", "mpd", "hyprland/workspaces"], // Sets modules for the left of the bar
    "modules-center": ["hyprland/window"], // Set modules for the center of the bar
    "modules-right": ["tray", "network","bluetooth","pulseaudio","battery"], // Set modules for the right of the bar

    "include": [".config/waybar/modules.jsonc"], // Include the module config
  },

  {
    "layer": "top", // Waybar on highest layer so tooltips go over windows
    "output": "HDMI-A-1", // Set output to secondary monitor
    "height": 27, // Set height to avoid jumping due to active workspace indicator

    "modules-left": ["clock", "mpd", "hyprland/workspaces"], // Sets modules for the left of the bar
    "modules-center": ["hyprland/window"], // Sets modules for the center of the bar
    "modules-right": ["tray", "network","bluetooth","pulseaudio","battery"], // Sets modules for the right of the bar

    "include": [".config/waybar/modules.jsonc"] // Include the module config
  }
]
